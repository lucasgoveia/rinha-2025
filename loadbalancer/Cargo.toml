[package]
name = "loadbalancer"
version = "0.1.0"
edition = "2024"

[dependencies]
pingora = { version = "0.5", features = [ "lb" ] }
async-trait = "0.1"
tokio = "1.47.0"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
